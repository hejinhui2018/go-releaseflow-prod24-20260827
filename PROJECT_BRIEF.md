# Project Brief

ReleaseFlow records the lifecycle of internal release packets for a software delivery team. A packet starts as a draft, receives a submit record, is reviewed and approved, and finally gets published or rolled back with a persisted audit trail.

The product exists so the release lead can recover the current release picture after a restart, inspect the sequence of state transitions, and export a compact report for downstream handoff.

